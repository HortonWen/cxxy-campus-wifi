# 成贤学院 Student_CX 校园网登录工具（Windows）

解决 Windows 电脑连上 `Student_CX` 后不弹认证页、每次都要手动找页面输入账号密码的问题。

## 文件说明

| 文件 | 作用 |
| --- | --- |
| `启动-成贤校园网登录.bat` | 双击打开图形界面（日常就用这个） |
| `CXXY-CampusNet.ps1` | 主程序，可命令行调用 |
| `创建开机自启.bat` | 双击后开机自动尝试连接并登录（需先保存密码） |

## 使用方法

1. 双击「启动-成贤校园网登录.bat」。
2. 填入门户账号（校园卡号）和门户密码，出口线路一般选「校园内网」，已办理电信/移动宽带可对应选择。
3. 点「连接并自动登录」：程序会先连上 `Student_CX`，再尝试自动认证；成功后即可上网。
4. 如果自动认证不成功，程序会自动弹出认证页面，手动输一次即可。也可以点「只打开认证页面」直接手动登录。

## 关于密码保存

默认不保存密码，每次打开都手动输入。只有勾选「本机加密保存密码」后，密码才会用 Windows 账户级加密（DPAPI）保存在本机，仅当前登录账户能解密，不会以明文写入任何文件。建议只在个人电脑上勾选。

## 开机自启

先勾选保存密码，再双击「创建开机自启.bat」。之后每次开机，程序会在后台尝试连接并自动登录；没有保存密码时只负责连接并打开认证页。取消自启：删除“启动”文件夹里的 `CXXY-Net-Auto.lnk`。

## 常见情况

- 提示“账号不存在/用户名或密码不正确”：用的是门户账号和门户密码，不是学号。
- 提示“unbind isp uid”：出口选了电信/移动，但没有在自助服务里绑定运营商宽带账号。
- 提示“登录超限”：同一时间最多 3 台设备在线，到自助服务 `http://211.65.40.6:8080/Self` 注销不用的设备。
- 有线网卡插着网线时认证页可能弹不出来，拔出网线后再试。

## 命令行（可选）

```text
powershell.exe -NoProfile -ExecutionPolicy Bypass -File CXXY-CampusNet.ps1 -Mode Check
powershell.exe -NoProfile -ExecutionPolicy Bypass -File CXXY-CampusNet.ps1 -Mode Login -User 校园卡号 -Pass 密码
```

## 说明

自动登录优先按认证页面的真实表单提交，失败后会尝试 Dr.COM ePortal 常见接口；两种都不行时兜底打开认证页手动登录。工具按学校公开指南（认证网关 `211.65.40.6`、触发地址 `6.6.6.6`）编写，请在校园网络环境下实测；如页面结构有变，告诉我现象即可调整。
